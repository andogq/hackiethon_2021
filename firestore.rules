rules_version = '2';
service cloud.firestore {
    match /databases/{database}/documents {
        match /exercises/{exercise} {
            allow read;
        }
        match /users/{user_id} {
            allow read, write, create: if request.auth != null && request.auth.uid == user_id;
        }
    }
}
